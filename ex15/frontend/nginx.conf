events {}

http {
    upstream backend {
        server backend-service:8080;
    }

    server {
        listen 80;
        root /usr/share/nginx/html;

        location / {
            index index.html;
        }

        location /api/ {
            proxy_pass http://backend;
        }
    }
}
